# Milestone 2 — Generic Infrastructure (CONFIRMED FINAL)

Incorporating your two latest mandatory requirements:

1. **Zero breaking changes** — Every rename/move is performed atomically in this milestone: the symbol is renamed AND all its consumers are updated in the same pass, and `npx tsc --noEmit` stays green throughout. No intermediate broken state.
2. **Generic libs fully independent of Firebase business entities** — The 5 core generic libs (`approvals`, `audit`, `kpi-scoring`, `idempotency`, `db-validation`) expose ONLY reusable primitives/interfaces. No RTDB collection names, no entity mappings, no Quality behavior leak into them. Concretely:
   - `writeAudit()` now requires the caller to pass `collection` (e.g. Quality passes `'qualityAuditLog'`) — the lib hardcodes NO collection names.
   - `validateForeignKeys`/`assertEntityExists` take `table` as a parameter — the lib hardcodes NO entity names.
   - `dedupByClientRequest` takes `collection` as a parameter.
   All RTDB collection names and entity behavior stay in the Quality service/API layer.

Everything else from the approved plan stands. Deliverables at the end: architecture report, public API summary, dependency graph, tsc/lint/test results, remaining tech debt, M3 recommendations. No Milestone 3.

Proceeding to write code.