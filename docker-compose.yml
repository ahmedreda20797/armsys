services:
  arm-erp:
    build: .
    container_name: arm-erp
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    restart: unless-stopped
    # Resource limits for free tiers
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 256M